# unique identifier for the challenge
id: example

title: example

flag_file: ./flag

points: 500

description: |-
    example **description** should support *markdown*

    ```
    including code blocks (no syntax highlighting)
    ```

    and tables work too

    Author: example

files:
    - ./relative/path/to/file
    - ./that/the/user/is/provided
